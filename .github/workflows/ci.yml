on: push 
jobs: 
  probar: 
    runs-on: ubuntu-latest 
    steps: 
      - uses: actions/checkout@v4 
      - uses: actions/setup-java@v6 
        with: 
          distribution: temurin 
          java-version: 21 
      - run: ./gradlew build
      - run: echo "La clave es ${{ secrets.MI_CLAVE }}" 